import React from 'react';

const ErrorIndicator = () => {
    return (
        <div className="note d-flex flex-column justify-content-center align-items-center h-100">
            <span>Something goes wrong with the app.</span>
            <span>We have already started to fix it,and soon,it will work in normal way.</span>
        </div>
    )
}

export default ErrorIndicator;